'use strict'; 

function calcCub (numb) {
	return numb ** 3;
}

let res = calcCub (3); 
console.log (res); 

//==========================

function calсSqrt (numb) {
	return Math.sqrt(numb);
}

console.log (calсSqrt(3) + calсSqrt(4)); 